# UITableView Demo 📱

A minimal project showing how to build a list entirely in code.

### Features
* **Zero Storyboards**: The TableView is created and framed programmatically.
* **Protocols**: Uses `UITableViewDataSource` to provide text and `UITableViewDelegate` to handle taps.
* **Clean Code**: Everything is contained within `ViewController.swift` for easy reading.
