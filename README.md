# Tip Calculator
A tip calculator app built using programmatic Auto Layout and reactive UI updates.

### Highlights
* **Auto Layout**: Uses `UIStackView` and `NSLayoutConstraint` for a responsive interface.
* **Dynamic Styling**: The result text color changes dynamically based on the tip percentage.
